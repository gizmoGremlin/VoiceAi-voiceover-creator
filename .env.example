# Voice.ai API Key — required for real TTS rendering.
# Get yours at https://voice.ai (or use --mock to skip).
VOICEAI_API_KEY=

# Optional: override the API base URL (defaults to https://api.voice.ai/v1)
# VOICEAI_API_BASE=https://api.voice.ai/v1
